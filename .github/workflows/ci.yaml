name: Build and Test with Docker Compose

on:
  push:
    branches: [main, development]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run make to build the docker images
        run: make

      - name: Start docker compose services (ignore exit code because timescaledb needs time to start)
        run: docker compose --env-file .env.example up -d || true

      - name: Wait for services to become healthy
        run: sleep 100

      - name: Show the status of the containers
        run: docker ps -a

      - name: Show the logs of the containers
        run: docker compose logs

      - name: Tear down docker compose
        if: always()
        run: docker compose down
